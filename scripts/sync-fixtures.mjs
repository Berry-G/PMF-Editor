/**
 * 목적: `docs/fixtures/` 를 게임 저장소의 사본 폴더로 복사한다. (M5 에서 구현)
 * 왜 이 구조인가: 픽스처의 원본은 이 저장소이고 게임 레포는 사본을 갖는다 — 게임 레포 단독으로
 *   EditMode 테스트가 돌아야 하기 때문이다(사용자 확정 2026-09-03). 사본은 **스크립트로만**
 *   갱신한다. 손으로 복사하면 어긋난 채로 오래 간다.
 * 바꾸면 안 되는 것: 복사 방향(에디터 → 게임). 반대로 돌리면 원본이 둘이 된다.
 * 근거: SDD-06 §3 [D-06-03], SDD-05 §4 [D-05-04], SDD-07 M5 [D-07-06]
 */

// 왜 아직 비어 있나: 픽스처 파일 자체가 M1 에서 생기고, 게임 쪽 사본 폴더의 확장자 규칙
//   (`.toon.txt` 로 둘지 TextAsset 으로 둘지)은 M5 에서 Unity 임포트 동작을 보고 정한다.
//   지금 아무렇게나 정해 두면 M5 에서 두 번 일한다.
console.log('[sync-fixtures] M5 에서 구현한다 (SDD-07 M5). 지금은 하는 일이 없다.');
